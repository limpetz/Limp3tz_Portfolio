node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Original full-size images, kept on disk for re-export only.
# The app ships the compressed .webp versions sitting next to them.
.originals/

# Sprite-pack sources: individual frames, previews, demo files, WebP twins and
# generation metadata. The app ships only the four indexed-PNG sheets plus the
# walk JSON (240px pack), and the compact legacy walk sheet.
src/assets/images/sprite/.originals/

# Freebuff agent workspace
.freebuff/
